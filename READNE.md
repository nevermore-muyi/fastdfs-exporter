You can clone the repository yourself and build using `make`:

```
$ mkdir -p $GOPATH/src/github.com/prometheus
$ cd $GOPATH/src/github.com/prometheus
$ git clone https://github.com/ruanchen/fastdfs-exporter
$ cd prometheus
$ make build
$ ./fastdfs-exporter
```

